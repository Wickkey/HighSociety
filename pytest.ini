[pytest]
testpaths = tests
python_files = test_*.py
# A hung test (stuck socket/thread) should fail loudly, not silently eat
# CI/dev time forever -- thread method so it can interrupt a blocked
# socket call, not just signal-based (which a background thread can swallow).
timeout = 60
timeout_method = thread
